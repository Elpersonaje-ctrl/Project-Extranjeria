RootWordmark — a large-format "RAÍZ" lockup with thin, near-invisible root-line threads (forest + gold, ~16–20% opacity) branching downward behind the letters. For big brand moments only (footer signature, section dividers) — use the compact `SealLogo` for the navbar/inline mark.

```jsx
<RootWordmark width={420} />
<RootWordmark width={320} inverse /> {/* on a dark forest background */}
```
