/**
 * @startingPoint section="Navigation" subtitle="Wordmark: dashed seal + RAÍZ serif logotype" viewport="700x120"
 */
export interface SealLogoProps {
  showSubtext?: boolean;
  /** Use on dark/forest backgrounds (e.g. footer). */
  inverse?: boolean;
}
