SealLogo — the brand's wordmark. No standalone logo file exists in the source repo; this dashed ink-seal + serif "RAÍZ" + thin subtext lockup, built from inline SVG, IS the mark.

```jsx
<SealLogo />
<SealLogo showSubtext={false} />
<SealLogo inverse /> {/* for the dark footer */}
```
