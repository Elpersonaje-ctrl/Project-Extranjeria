/**
 * @startingPoint section="Navigation" subtitle="Large wordmark with root-line backdrop — footers, dividers, brand moments" viewport="700x300"
 */
export interface RootWordmarkProps {
  /** Use on dark/forest backgrounds. */
  inverse?: boolean;
  /** Rendered pixel width; height follows the fixed backdrop aspect ratio. */
  width?: number;
}
