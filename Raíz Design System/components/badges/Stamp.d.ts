/**
 * @startingPoint section="Badges" subtitle="Ink-stamp APROBADO / RECHAZADO marker" viewport="700x160"
 */
export interface StampProps {
  tone?: 'approved' | 'rejected';
  children?: React.ReactNode;
}
